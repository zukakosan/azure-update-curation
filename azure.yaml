# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: azure-update-curation
metadata:
  template: azure-update-curation@0.0.1
services:
  web:
    project: ./site
    dist: .
    language: js
    host: staticwebapp
